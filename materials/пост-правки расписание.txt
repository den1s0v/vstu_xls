# последние разы такого не встречаю, но на всякий случай проверяю
# встречалось внутри дат, сейчас осталось у имён преподавателей (не критично)
..
;

# такое всё ещё встречается, меняю на \d\d.05.2026
.15.2025

# проверяю на склеенные даты
\d\d.\d\d.\d\d.\d\d
# тоже самое
\d\d.\d\d\d\d.\d\d

# были расписания с такими датами
2023-2024
# когда 2ой семестр начался, всё ещё встречался 1ый
1 семестр

# пока все названия факультетов заглавными буквами
# а фастив такой единственный
ФАСТиВ

"Учебные занятия магистратура 1 курса ФАСТиВ бакалавриат"
одновременно мага+бака в названии



ФАЙЛ schedule_metadata:

ВМЦЭ есть только бакалавриат, сам создаю магу

Не должны бать пустыми (в самом начале файла у фастива):
"schedule_template_metadata_faculty_shortname": ""
"department_shortname": ""






===========================
===========================
===========================



Реализовал план полностью, **без изменений файла плана** — только код в `vstu.py`.

Изменения внесены в `src/vstuxls/export/vstu.py`:

- Добавлены верхнеуровневые константы:
  - `CURRENT_ACADEMIC_YEAR = "2025-2026"`
  - `CURRENT_SEMESTER = "2"`
  - `CURRENT_SEMESTER_START_DATE`
  - `CURRENT_SEMESTER_END_DATE`
- `build_schedule_metadata()` переведён на эти константы вместо хардкодов.
- Добавлен блок пост-обработки `apply_post_fixes(...)`, который теперь вызывается из `export_schedule_document_as_json(...)`.
- Нормализация `title`:
  - исправление старых диапазонов годов на `CURRENT_ACADEMIC_YEAR`;
  - исправление `1 семестр` -> текущий `2 семестр`;
  - устранение конфликтов scope (`магистратура` + `бакалавриат`) с приоритетом scope из metadata;
  - нормализация `ФАСТиВ` -> `ФАСТИВ`.
- Нормализация дат:
  - исправление инверсии даты типа `12.15.2025` -> `15.12.2025`;
  - разбор склеек вида `16.04.17.05`;
  - унификация в `%d.%m.%Y`.
- Очистка артефактов в занятиях:
  - `teachers`: удаление лишних двойных точек (`доц..`, `А.А..`);
  - `places`: извлечение «утёкших» дат в `holds_on_date`, очистка шумовых значений.
- Усилена обработка структур `m1/m2/...` в `_extract_lessons()`:
  - в `extract_discipline()` добавлена безопасная распаковка таких словарей;
  - в `resolve_groups()` добавлен рекурсивный fallback по `m1/m2/...`.
  - Это предотвращает падения/пропуски при подобных структурах и не пропускает сырые `m*`-узлы в экспорт.

Проверки, которые выполнил:

- Синтаксис: `python -m compileall src/vstuxls/export/vstu.py` — ок.
- Линтер по изменённому файлу — без ошибок.
- Смоук-тесты функций нормализации (title/даты/places/teachers) — ок.
- Проверка `data/output` на `m2`-артефакты (`"m2":` / `m2:`) — не найдено.

Следующим шагом можно запустить целевой прогон `demo/batch_demo.py` на выбранной подпапке и дать короткий отчёт «до/после» по 3-5 конкретным файлам из `data/output`.


