[project]
name = "vstu-xls"
version = "0.1.0-beta.1"
description = "2D-grammar parser developed primarily for VSTU schedules in spreadsheet format"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
